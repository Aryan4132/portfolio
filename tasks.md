# Tasks: Portfolio Cloudflare Optimization

- [ ] 1. Create `public/_redirects` file for Cloudflare Pages SPA routing
- [ ] 2. Create `public/_headers` file for Cloudflare security & caching headers
- [ ] 3. Create `public/favicon.svg` for brand favicon
- [ ] 4. Update `package.json` with project name and clean metadata
- [ ] 5. Update `vite.config.ts` for Rollup vendor chunking (`three`, `motion`, `lucide-react`, `animejs`)
- [ ] 6. Update `index.html` with OpenGraph, Twitter Cards, Schema.org JSON-LD, and Favicon link
- [ ] 7. Update `src/App.tsx` with floating Scroll-to-Top button & interactive quick actions
- [ ] 8. Run `npm run lint` and `npm run build` to verify optimization
- [ ] 9. Commit & push changes to GitHub `main` branch
